# Web Framework & Server
fastapi==0.111.0 # Or latest stable version as of Apr 2025
uvicorn[standard]==0.29.0 # Or latest stable

# Database (PostgreSQL)
sqlalchemy==2.0.29 # ORM
alembic==1.13.1 # Migrations
psycopg2-binary==2.9.9 # Postgres Driver (easier install)
python-dotenv==1.0.1 # For loading .env files

# Cache/Queue (Redis)
redis==5.0.4

# Security & Auth
passlib[bcrypt]==1.7.4 # Password Hashing
python-jose[cryptography]==3.3.0 # JWT Tokens
cryptography==42.0.7 # Encryption

# HTTP Client (for calling external APIs/LLMs)
httpx==0.27.0

# LLM Clients (install base versions now, specific versions later if needed)
openai==1.23.6 # Example
anthropic==0.50.0 # Example 